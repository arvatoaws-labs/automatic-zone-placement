apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "automatic-zone-placement.fullname" . }}-test-connection"
  labels:
    {{- include "automatic-zone-placement.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "automatic-zone-placement.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
