apiVersion: v1
kind: Service
metadata:
  name: {{ include "automatic-zone-placement.fullname" . }}
  labels:
    {{- include "automatic-zone-placement.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "automatic-zone-placement.selectorLabels" . | nindent 4 }}
